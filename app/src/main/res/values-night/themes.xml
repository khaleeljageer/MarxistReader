<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark theme so splash (window background) is dark when system is in dark mode. -->
    <style name="Theme.MarxistReader" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/splash_background_light</item>
    </style>
</resources>
